import React from 'react'

const Wishlist = () => {
    // const [List, setList] = useState([])
    return (
        <div>
            {/* {
                (list == null) ? <h1>wishlist Not found</h1> : <h1>Your List are here</h1>
            } */}
        </div>
    )
}

export default Wishlist
